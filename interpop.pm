sub popIDs
{my($filename) = @_;
if $line ~ "pop1" {
    split{$line};
    for($x=0; $x>len(line); x++){
      my $pop1[$x] = $split_line
    }
}
elsif $line ~ "pop2" {--ditto--}


sub interPopStats
{my($filename) = @_;

#inloop
#in file

#outloop
